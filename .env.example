# Local mobile app -> local API
# Replace the host with the machine running the API when testing on a physical device.
EXPO_PUBLIC_API_URL="http://192.168.1.10:5000"

# Google Maps key used by the native mobile build and directions requests.
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=""
